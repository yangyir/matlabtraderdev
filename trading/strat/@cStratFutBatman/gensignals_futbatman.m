function signals = gensignals_futbatman(obj)
    %note:the batman strategy is still self-mannual, i.e. the open position
    %is done via mannual trades
    variablenotused(obj);
    signals = {};
end